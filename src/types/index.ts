export type User = {
    avatar_url: string;
    html_url: string;
    name: string;   
    email: string;
    login: string;
}

export type Repos = {
    name: string;   
    html_url: string;
    description: string;
    language: string;
}